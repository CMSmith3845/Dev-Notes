def increment(number, by):
    return number + by


print(increment(2, by=1))  # A more simple way to write the code below, also we are using a keyword argument to
# make it more clear for anyone reading our code that the 1 is the parameter 'by'

# result = increment(2, 1)
# print(result)
