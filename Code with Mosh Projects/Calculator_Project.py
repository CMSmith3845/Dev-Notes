First = float(input('First '))
Second = float(input('Second ')) #can also covert here at the time of taking the input, less code lines this way
sum = float(First) + float(Second) #can convert to a float here or check above ^, this would be redundant at this point.
print('Sum:', sum)
